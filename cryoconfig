# This is just a draft of how the `cryoconfig` file will look.
# TODO: I will need to make a new lexer/parser for this.
WATCH_IGNORE = [
    "./build/*", 
    "./src/bin/*",
    "./tests/*",
    "./build.sh",
    ".gitignore",
    ".git",
    ".vscode",
]

CRYO_LIBS = [
    "std",
    "fs",
    "net",
]
