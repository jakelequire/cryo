namespace Main;

extern function printStr(str: string) -> void;
extern function printInt(num: int) -> void;
extern function sys_exit(code: int) -> void;
extern function fib(n: int) -> void;


public function fibonacci(n: int) -> void {
    mut a: int = 0;
    mut b: int = 1;
    mut c: int = 0;

    while (n > 0) {
        c = a + b;
        a = b;
        b = c;

        printInt(a);
        n = n - 1;
    }

    return;
}


public function main() -> void {
    fibonacci(10);

    printStr(" ");
    printStr("Correct Output: ");
    fib(10);

    return;
}
